// core
const token = ""  // токен
// Токен получать тута: https://vkhost.github.io/ (тыкать на Kate Mobile)
// Если вы выберите другие приложухи, то не будет спамить
// Сорян
const group_id = 0; // Сюда id группы без минуса. Если вы с юзера, то можно не указывать.
const count = 0 // кол-во названий
const toCount = 0 // число, которое больше нули
const chatId = 0 // ID чата
const time = 0 // время для того, что-бы вк не спалил
module.exports = {
	token,
	group_id,
	count,
	toCount,
	chatId,
	time
}